[spine]
{% for i in spines %}{{i.inst.state().network['eth0']['addresses'][0]['address']}} router_ip=10.0.254.{{range(1, 254) | random}} router_advertise='[]' reserved_ports='["eth0"]' asn=65500 ansible_ssh_private_key_file=private.key testnet=True
{% endfor %}

[leaf]
{% for i in leafs %}{{i.inst.state().network['eth0']['addresses'][0]['address']}} router_ip=10.0.200.{{range(1, 254) | random}} router_advertise='[]' reserved_ports='["eth0"]' ansible_ssh_private_key_file=private.key testnet=True
{% endfor %}
